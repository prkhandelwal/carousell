# Carousell Marketplace CLI (Java)

Implements the Carousell marketplace CLI as per assignment spec.

## Environment

- Java JDK 11 (Amazon Corretto)
- No external libraries, only Java standard library

## Build

```bash
./build.sh
